server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/plain text/css application/javascript application/json image/svg+xml;

    # Hashed build assets (Vite fingerprints these) are safe to cache forever.
    location /assets/ {
        add_header Cache-Control "public, max-age=31536000, immutable";
        try_files $uri =404;
    }

    # index.html must always be revalidated, or a redeploy behind a CDN/proxy cache
    # could keep serving a stale bundle reference.
    location = /index.html {
        add_header Cache-Control "no-cache";
    }

    # No client-side router in this app today, but falling back to index.html for
    # any unmatched path is a harmless default and avoids a raw nginx 404 page.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
